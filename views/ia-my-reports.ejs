<%- include("partials-header", { title: "My IA Reports" }) %>

<section class="toolbar">
  <h1>My IA Reports</h1>
  <a href="/ia" class="btn btn-ghost">Back to IA Dossiers</a>
</section>

<section class="card">
  <% if (!reports || reports.length === 0) { %>
    <p class="muted">You have not created any IA reports yet.</p>
  <% } else { %>
    <div class="report-list">
      <% reports.forEach((report) => { %>
        <article class="report-list-item">
          <div>
            <p><strong>Subject:</strong> <%= report.subject_name %></p>
            <p class="muted"><%= report.subject_faction %> | <%= report.subject_affiliation %></p>
            <p class="muted">Incidents: <%= report.incident_count %> | Updated: <%= report.updated_at %></p>
          </div>
          <div class="toolbar-actions">
            <a class="btn btn-ghost" href="/ia/<%= report.dossier_id %>">IA Dossier</a>
            <a class="btn" href="/ia/reports/<%= report.id %>">Open IA Report</a>
          </div>
        </article>
      <% }) %>
    </div>
  <% } %>
</section>

<%- include("partials-footer") %>
